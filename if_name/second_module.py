# the name of the import matches the file name (minus the .py)
# these two files MUST BE IN THE SAME DIRECTORY
import first_module

print("Module #2 Name=", __name__)

#below will be description if i DID want to call that main()function, this will be how to do it

# the name of the import matches the file name (minus the .py)
# these two files MUST BE IN THE SAME DIRECTORY
# "uncomment this line" import first_module

# "uncomment this line" first_module.main() # ADD THIS LINE

# "uncomment this line" print("Module #2 Name=", __name__)

